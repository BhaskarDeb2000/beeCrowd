#include <stdio.h>
int main()
{
    int A, B, C, D, DIFERENCE;
    scanf("%d\n%d\n%d\n%d", &A, &B, &C, &D);
    DIFERENCE = (A * B) - (C * D);
    printf("DIFERENCA = %d\n", DIFERENCE);
    return 0;
}